import React from "react";

const Content = () => {
  return <h1>Hello World</h1>;
};

const MainSection = () => {
  return (
    <main>
      <Content />
    </main>
  );
};

export default MainSection;
